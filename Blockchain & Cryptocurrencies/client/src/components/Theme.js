import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
    palette: {
      primary: {
        main: "#921313",
      },
      secondary: {
        main: "#921313",
      },

    },
  });
  
  export default theme;